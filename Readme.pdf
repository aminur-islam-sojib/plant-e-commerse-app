 <motion.img
              key={card.id ?? idx}
              src={card.url}
              alt={card.title ?? card.url}
              className="rounded-2xl shadow-md"
              initial={false}
              animate={positionFor(idx)}
              variants={imageVariants}
              transition={{ type: 'spring', stiffness: 260, damping: 30 }}
              style={{ position: 'absolute', top: '10%', height: '200px' }}
            />